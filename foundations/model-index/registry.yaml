schema_version: 5

entity_kind_definitions:
  model-family: "以可追溯版本谱系维护的一组完整或近完整模型。"
  architecture-family: "主要由可复用结构机制和接口定义的架构谱系。"
  component-family: "边界明确、可嵌入其他架构的组件谱系。"
  method-family: "主要由训练、对齐、适配或推理方法定义的谱系。"

usage_scope_definitions:
  complete-solution: "能够在给定组合和配置下直接完成一个或多个视觉任务。"
  reusable-module: "能够作为其他系统中的可复用模块。"
  pretraining-source: "主要作为权重、表征或初始化来源被下游使用。"

module_role_definitions:
  backbone: "为具体任务系统提供可由 neck、head、decoder 或其他消费者使用的特征；输出可以是单尺度、分层或并行多分辨率。"
  visual-encoder: "将图像或视频编码为可供下游消费的表示。"
  neck: "在主要特征提取与任务输出之间转换或融合特征。"
  head: "将上游特征转换为局部任务输出。"
  task-decoder: "通过 query、token 或迭代解码产生任务结果。"

families:
  - id: yolo
    name: YOLO
    path: families/yolo
    entity_kind: model-family
    usage_scopes: [complete-solution]
    module_roles: []
    learning_paradigms: [supervised]
    tasks: [object-detection, 2d-landmark-localization]
    system_contexts: []
    relations: []
    status: working
    evidence_status: partial
    structure_review: accepted
    content_review: pending
    owner_use: confirmed
    scope: "主要、可追溯的实时检测分支及其正式 pose 等任务扩展；不穷举所有带 YOLO 名称的项目，也不把 COCO pose 权重当作人脸关键点证据。"
    authority_note: "YOLOv3 之后没有覆盖全部分支的单一维护组织；任务扩展必须绑定具体维护方、代码 revision、head、schema 和导出合同。"
    next_gate: "YOLO 理论结构已验收；YOLO26 Pose 进入真实选型前需固定 custom keypoint schema、训练数据、export branch 与目标硬件。"

  - id: detr
    name: DETR
    path: families/detr
    entity_kind: model-family
    usage_scopes: [complete-solution]
    module_roles: []
    learning_paradigms: [supervised]
    tasks: [object-detection, 2d-landmark-localization]
    system_contexts: []
    relations: []
    status: working
    evidence_status: partial
    structure_review: pending
    content_review: pending
    owner_use: not-confirmed
    scope: "端到端集合预测及其多尺度、去噪、实时、开放词汇和 RF-DETR keypoint 扩展；不把所有 Transformer 检测/姿态模型或自监督 DINO 混入。"
    authority_note: "原始 DETR 与后续分支来自不同组织；RF-DETR Keypoint 当前为 Roboflow Preview，不能作为全部 DETR family 的稳定 pose 接口。"
    next_gate: "先审查 family 与 keypoint extension 边界；真实采用需固定实现、schema、matching/output、runtime 与任务 Oracle。"

  - id: mobilenet
    name: MobileNet
    path: families/mobilenet
    entity_kind: architecture-family
    usage_scopes: [reusable-module, pretraining-source]
    module_roles: [backbone, visual-encoder]
    learning_paradigms: [supervised]
    tasks: []
    system_contexts: []
    relations: []
    status: working
    evidence_status: partial
    structure_review: pending
    content_review: pending
    owner_use: not-confirmed
    scope: "Google MobileNetV1–V4 及其第一方硬件分支；不把 ShuffleNet、GhostNet、EfficientNet、MobileOne、RepViT 或通用轻量网络混入。"
    authority_note: "论文定义架构主线；固定 TensorFlow Models revision 定义可定位官方实现。官方概览与代码可能不同步，不能只依赖摘要页判断版本完整性。"
    next_gate: "先审查家族边界、来源责任和停止维护规则；真实采用需固定实现、任务消费者、runtime 与目标硬件后进入受控比较。"

  - id: pfld
    name: PFLD
    path: families/pfld
    entity_kind: model-family
    usage_scopes: [complete-solution]
    module_roles: []
    learning_paradigms: [supervised]
    tasks: [2d-landmark-localization]
    system_contexts: []
    relations: []
    status: working
    evidence_status: partial
    structure_review: pending
    content_review: pending
    owner_use: not-confirmed
    scope: "论文定义的轻量 face-ROI direct-coordinate landmark family；不把所有 MobileNet 人脸关键点网络、社区 fork 或整图 pose 系统混入。"
    authority_note: "论文与作者项目定义 PFLD 主线；公开 PyTorch/ONNX/NCNN 实现来自不同维护者，只负责自身结构、权重与转换。"
    next_gate: "先审查 paper-vs-community 边界；真实采用需固定 point schema、ROI/preprocess、implementation、checkpoint、visibility contract 与目标硬件。"

  - id: hrnet
    name: HRNet
    path: families/hrnet
    entity_kind: architecture-family
    usage_scopes: [reusable-module, pretraining-source]
    module_roles: [backbone]
    learning_paradigms: [supervised]
    tasks: []
    system_contexts: []
    relations: []
    status: working
    evidence_status: partial
    structure_review: pending
    content_review: pending
    owner_use: confirmed
    scope: "并行多分辨率与重复融合的 HRNet architecture family；face-alignment heatmap 是具体组合，不把所有高分辨率网络或 heatmap 方法混入。"
    authority_note: "HRNet 论文/组织定义架构；固定官方 facial-landmark revision 定义历史 W18 heatmap 实现，现代 MMPose 配置属于第三方组合。"
    next_gate: "真实 DMS 比较需固定 HRNet variant、input/heatmap、loss、codec/decoder、visibility head、ROI 和目标硬件。"

  - id: dino-self-supervised
    name: DINO (self-supervised)
    path: families/dino-self-supervised
    entity_kind: method-family
    usage_scopes: [pretraining-source]
    module_roles: []
    learning_paradigms: [self-supervised]
    tasks: []
    system_contexts: []
    relations: []
    status: working
    evidence_status: partial
    structure_review: pending
    content_review: pending
    owner_use: not-confirmed
    scope: "Meta/FAIR DINO、DINOv2、register 扩展与 DINOv3 的自监督视觉表征方法谱系；不把 DETR 检测器 DINO、ViT/ConvNeXt 架构或任意下游 DINO 项目混入。"
    authority_note: "论文定义方法主线；固定官方仓库和模型卡定义可加载 backbone、token/feature 接口、权重与许可。动态 README 中的领域项目和 post-hoc 适配只建立关系。"
    next_gate: "先审查方法边界、数据/权重/许可和停止维护规则；真实采用需固定 architecture、checkpoint、consumer、adaptation budget、runtime 与任务 Oracle 后比较。"

  - id: clip
    name: CLIP (OpenAI)
    path: families/clip
    entity_kind: model-family
    usage_scopes: [complete-solution, reusable-module, pretraining-source]
    module_roles: [visual-encoder]
    learning_paradigms: [vision-language-alignment]
    tasks: [classification]
    system_contexts: []
    relations: []
    status: working
    evidence_status: partial
    structure_review: pending
    content_review: pending
    owner_use: not-confirmed
    scope: "OpenAI 公开的九个配对图文 checkpoint 及其固定图像/文本/相似度接口；不把 OpenCLIP、SigLIP、ALIGN、EVA-CLIP、MetaCLIP、DFN、下游开放词汇系统或通用 VLM 混入。"
    authority_note: "论文与 OpenAI model card 定义方法、数据披露和使用边界；固定 openai/CLIP revision 定义 checkpoint、preprocess、tokenizer、encoder 和 logit contract。"
    next_gate: "先审查 family 边界、prompt/taxonomy 版本化、model-card 限制和停止维护规则；真实采用需固定 checkpoint、taxonomy、prompt、consumer、runtime 与任务 Oracle 后比较。"

  - id: turingvit
    name: TuringViT
    path: families/turingvit
    entity_kind: architecture-family
    usage_scopes: [reusable-module]
    module_roles: [backbone, visual-encoder]
    learning_paradigms: []
    tasks: []
    system_contexts: []
    relations: []
    status: working
    evidence_status: partial
    structure_review: pending
    content_review: pending
    owner_use: not-confirmed
    scope: "面向高分辨率图像与视频的视觉编码器；关注 TLA、动态分辨率、数据治理和硬件协同边界。"
    authority_note: "当前由小鹏官方项目页和论文定义；代码、权重与独立复现尚未公开。"
    next_gate: "等待代码或权重、第三方复现及真实目标硬件结果，再判断工程采用价值。"
