# TODO

这里维护已经确认、但尚未进入实施的仓库级方向。具体工作开始后，应转入对应主题目录或独立分支；本文件只保留状态、边界、依赖和验收出口，不扩展成方案正文，也不预建空目录。

## 执行规则

- 数字编号表示长期方向，字母子项表示可独立完成、验收和回退的工作包。
- 一条分支原则上只完成一个子项；若两个子项不能独立验收，先合并定义，再开始内容建设。
- 同一事实只有一个权威条目；跨模块、学习范式、任务和系统应用的关系使用链接，不复制正文。
- family 页面只有通过 Foundations 新增门槛后才创建；待办存在不等于提前创建目录。
- 真实案例必须区分公开资料、第三方结果、本人独立验证和公司/IP 受限观察。
- 父项只有在必要子项完成、且完成标志得到证据支持后才关闭；不能以“文档已写”代替验证。

## 分类说明

以下分组是四个正交视角，不是互斥目录树：

1. 架构模块：可复用的网络部件及接口；
2. 学习、表征与对齐：模型如何获得、组织或对齐表征；
3. 任务与完整模型方案：端到端解决什么视觉问题；
4. 系统应用：视觉能力如何进入更大的产品或智能系统。

同一个对象可以出现在多个视角，但只能有一个权威条目维护事实，其他位置通过关系链接引用。例如 HRNet 的高分辨率结构归入架构模块，HRNet-based heatmap 与 PFLD 的实验比较归入关键点任务；Meta DINO 归入自监督表征，检测器 DINO 归入 Detection → DETR。

Backbone 与 visual encoder、head 与 task decoder 只作为相邻角色的关系入口，不表示它们是同义词。Adapter 与 neck/feature fusion 分开维护；视频表征学习与跟踪、事件时序判定也分开维护。

## Pending

### 1. 将 ProjectCollection 中仍有价值的内容融入 vision-workbench

- **来源**：[visionzoo/projectcollection](https://github.com/visionzoo/projectcollection)。
- **目标仓库**：[visionzoo/vision-workbench](https://github.com/visionzoo/vision-workbench)。
- **边界**：不整体复制旧仓；归档、删除或公开属于另行确认的动作。
- **关闭条件**：所有入选对象已逐项迁移或明确放弃，旧仓处置经过本人确认。

| 子项 | 独立交付物 | 验收出口 |
|---|---|---|
| 1A | ProjectCollection 内容盘点与去向清单 | 每项标明权威对象、证据/IP 状态、迁移/保留/放弃建议；不迁移文件 |
| 1B | 首批迁移顺序与依赖 | 只选择通过范围和去重检查的对象，并由本人确认 |
| 1C | 单对象迁移批次 | 每个对象单独分支；迁移后导航、反向链接和来源边界完整 |
| 1D | 旧仓处置决定 | 所有迁移完成后，单独确认保留、归档或删除；不与内容迁移混合 |

## 架构模块

### 2. Visual feature extraction：backbone 与 visual encoder

- **优先级**：高。
- **边界**：这是两个相邻模块角色的关系索引，不把它们当成同义词，不把完整模型强拆成三段，也不把跨论文指标直接排行。
- **关闭条件**：代表谱系均有通过门槛的权威条目，形成跨家族选型关系，并回链至少一个精度、速度和部署条件完整的受控案例。

| 子项 | 独立交付物 | 主要边界或依赖 |
|---|---|---|
| 2A | Backbone / visual encoder 比较契约与总索引 | 先定义角色差异、输入输出、尺度、预训练、精度、计算、内存、导出和量化口径；不批量建 family |
| 2B | ResNet 家族第一版 | 只维护可追溯谱系、残差机制、接口与选型边界 |
| 2C | MobileNet 家族第一版 | 与 ResNet 等共用 2A 口径，不复制通用部署知识 |
| 2D | EfficientNet 家族第一版 | 区分缩放方法、架构谱系和实现生态 |
| 2E | ConvNeXt 家族第一版 | 区分现代 CNN 机制与下游完整方案 |
| 2F | ViT 家族第一版 | 维护 patch/token 编码与全局注意力的核心边界 |
| 2G | Swin 家族第一版 | 维护层级与窗口机制，不并入 ViT 页面造成双重权威 |
| 2H | HRNet 家族第一版 | 维护高分辨率多分支机制；眼部关键点结果回链待办 10 |
| 2I | TuringViT 证据缺口收敛 | 补代码、权重、独立复现或明确长期不可得项；不重写已有页面 |
| 2J | 跨家族选型关系图 | 依赖 2A 和至少两个差异明显的家族条目，不做脱离条件的总排名 |
| 2K | 一个受控下游对照案例 | 固定主要非 backbone/encoder 变量，记录精度、速度、内存和部署条件 |

**当前下一工作包：2A。** 2B–2I 各自使用独立分支；2J、2K 在前置证据足够后启动。

### 3. Neck 与 feature fusion

- **边界**：只维护中间多层/多尺度特征的转换与融合；接口 adapter、跨模态 adapter 和参数高效 adapter 归待办 4。
- **关闭条件**：主要融合谱系边界清楚，并至少有一次固定上下游主要变量的消融。

| 子项 | 独立交付物 | 验收出口 |
|---|---|---|
| 3A | Neck / feature fusion 接口与比较契约 | 定义输入层级、尺度、通道、融合方向、算子和计时口径 |
| 3B | FPN 家族第一版 | 只维护其权威谱系与多尺度融合机制 |
| 3C | PAN/PANet 相关谱系第一版 | 先解决名称和实现归属，不把不同 PAN 变体混为一体 |
| 3D | BiFPN 家族第一版 | 维护双向融合和加权机制及其适用边界 |
| 3E | 一个受控 feature-fusion 消融 | 固定 backbone/encoder、head/decoder、数据和训练主变量 |

### 4. Adapter 与 interface adaptation

- **边界**：adapter 是关系角色，不预设单一模型族；分辨率/通道接口、跨模态连接和参数高效迁移分别维护。
- **关闭条件**：三类 adapter 的目标、训练边界和接口可区分，并有一个真实组合案例。

| 子项 | 独立交付物 | 验收出口 |
|---|---|---|
| 4A | Adapter 类型、接口与所有权边界 | 明确何时属于架构模块，何时主要属于训练/对齐范式 |
| 4B | 分辨率/通道 interface adapter | 限于工程接口适配，不扩成训练范式 |
| 4C | 跨模态 adapter | 与 CLIP/VLM 回链；不复制对齐模型正文 |
| 4D | 参数高效 adapter | 训练目标和方法回链对应学习条目；这里只维护模块接口 |
| 4E | 一个受控 adapter 组合案例 | 固定主要上下游变量，记录收益、参数、延迟和失效条件 |

### 5. Task output：head 与 task decoder

- **边界**：这是输出侧相邻角色的关系入口，不表示二者同义；检测、关键点和分割的任务特有方案分别归待办 9、10、13。
- **关闭条件**：输出链路中的对象职责可区分，并有一次固定上游表征的真实对照。

| 子项 | 独立交付物 | 验收出口 |
|---|---|---|
| 5A | Head / task decoder / loss / matcher / 后处理边界表 | 阻止“位于输出链路”被误写成“都属于 head” |
| 5B | 跨任务输出表征索引 | 只做分类、检测、关键点、分割之间的关系入口；任务细节不重复 |
| 5C | 一个固定上游表征的对照案例 | 明确输出表征、训练目标、解码、速度和误差差异 |

## 学习、表征与对齐

### 6. 自监督视觉表征 DINO 家族

- **边界**：只指 Meta 自监督谱系；检测器 DINO 归待办 9。
- **关闭条件**：第一方谱系、迁移方式和实际收益/代价均有证据。

| 子项 | 独立交付物 | 验收出口 |
|---|---|---|
| 6A | DINO / DINOv2 / DINOv3 第一方谱系与机制图 | 版本、数据、训练目标和披露边界可追溯 |
| 6B | 下游适配与选型说明 | 区分 encoder 架构收益与预训练范式收益 |
| 6C | 一个监督预训练对照实验 | 固定下游协议，报告迁移收益及训练、存储、部署代价 |

### 7. CLIP 家族与视觉—语言对齐

- **边界**：不把第三方 OpenCLIP 生态、通用 VLM 和 OpenAI CLIP 混成单一官方谱系。
- **关闭条件**：对齐机制、身份归属、适配方式和至少一个真实视觉任务验证完整。

| 子项 | 独立交付物 | 验收出口 |
|---|---|---|
| 7A | CLIP 权威谱系与生态边界 | 第一方模型、第三方复现和衍生生态分开 |
| 7B | 对齐、零样本、开放词汇与微调决策表 | 同时写明闭集专用模型更合适的条件 |
| 7C | 一个闭集与开放词汇对照案例 | 记录精度、类别变化成本、延迟和错误成本 |

### 8. 视频表征学习

- **边界**：这里只维护时空表征如何学习和迁移；多目标跟踪归待办 11，事件时序判定归待办 12。
- **关闭条件**：表征目标、输入采样、下游适配和相对单帧基线的真实收益均可验证。

| 子项 | 独立交付物 | 验收出口 |
|---|---|---|
| 8A | 视频表征问题、训练目标与谱系图 | 区分视频编码、跟踪关联和事件决策 |
| 8B | 输入采样、时空建模与下游适配说明 | 记录帧率、窗口、计算和延迟条件 |
| 8C | 一个相对单帧基线的真实案例 | 证明新增价值，并报告计算、状态和失效模式 |

## 任务与完整模型方案

### 9. Detection：YOLO、DETR 与检测器 DINO

- **优先级**：高。
- **边界**：YOLO 事实继续由现有条目维护；本项新增 DETR 谱系和跨方案选择，不复制 backbone 等通用机制。
- **关闭条件**：能够在同一问题约束下，对 YOLO、代表性 DETR 分支和检测器 DINO 做有证据的选择。

| 子项 | 独立交付物 | 主要边界或依赖 |
|---|---|---|
| 9A | DETR 核心机制与原始基线 | 集合预测、query、匹配、loss、收敛问题可追溯 |
| 9B | Deformable DETR 家族第一版 | 维护相对 DETR 的机制继承和变化 |
| 9C | DN-DETR 家族第一版 | 维护去噪训练机制，不与检测器 DINO 重复 |
| 9D | 检测器 DINO 家族第一版 | 明确继承关系，并与 Meta DINO 完全分开 |
| 9E | YOLO / 代表性 DETR / DINO 受控选型案例 | 统一数据、输入、硬件、精度和计时边界 |

### 10. 关键点与姿态估计

- **优先级**：高。
- **边界**：HRNet 架构事实归 2H；当前 HRNet-based heatmap 优于 PFLD 只是待补条件的系统观察。
- **关闭条件**：关键点输出、遮挡/可见性、评价和部署边界清楚，并完成眼部关键点真实对照。

| 子项 | 独立交付物 | 主要边界或依赖 |
|---|---|---|
| 10A | 关键点任务定义与评价契约 | 覆盖可见性、遮挡、局部目标、标注和指标口径 |
| 10B | Heatmap / 坐标回归 / SimCC 方案图 | 比较输出表征，不把代表实现等同于整个范式 |
| 10C | HRNet-based heatmap / PFLD 眼部关键点对照 | 补齐数据、输入、训练、规模、精度和速度；使用公开或独立可披露材料 |
| 10D | 不可见点、时序稳定与板端部署案例 | 依赖 10C；工程结论归 Engineering，Foundations 只回写可复用结论 |

### 11. 多目标跟踪

- **边界**：不把视频表征或事件状态机自动归入跟踪；它们只在具体方案需要时建立关系。
- **关闭条件**：跟踪问题、评价、状态接口和相对逐帧检测的真实收益均有证据。

| 子项 | 独立交付物 | 验收出口 |
|---|---|---|
| 11A | 跟踪问题、指标与方案图 | 明确检测关联、运动状态、ReID 和轨迹指标的职责 |
| 11B | 检测器与跟踪器接口契约 | 记录输入、状态、丢失恢复、延迟和部署边界 |
| 11C | 一个相对逐帧基线的真实案例 | 报告关联收益、延迟、状态和失效模式 |

### 12. 事件时序判定

- **边界**：解决状态变化和事件判定；不以“用了视频”替代任务定义，也不复制视频 encoder 知识。
- **关闭条件**：平滑、状态机、概率模型和学习式时序方案的选择边界清楚，并有公开或独立案例。

| 子项 | 独立交付物 | 验收出口 |
|---|---|---|
| 12A | 事件定义、时间窗与评价契约 | 明确事件标签、触发/恢复、误报漏报和延迟口径 |
| 12B | 时序判定方案图 | 区分规则平滑、状态机、概率模型和学习式模型 |
| 12C | 一个相对单帧判定的真实案例 | 报告误报/漏报、延迟、状态恢复和失效条件 |

### 13. 分割与可提示分割

- **边界**：语义、实例和可提示分割先分别建设，再做关系比较；SAM 不扩成通用基础模型百科。
- **关闭条件**：三类任务的输入输出、指标和成本可区分，并有一个真实流程证明分割相对检测框的新增价值。

| 子项 | 独立交付物 | 验收出口 |
|---|---|---|
| 13A | 分割任务与评价总契约 | 统一标注粒度、输出、指标、延迟和内存口径 |
| 13B | 语义分割方案第一版 | 保持任务权威入口，不批量罗列模型 |
| 13C | 实例分割方案第一版 | 与检测和语义分割建立关系但不复制正文 |
| 13D | 可提示分割 / SAM 家族第一版 | 覆盖提示接口、自动掩码和轻量化边界 |
| 13E | 检测框 / 传统分割 / 可提示分割真实对照 | 记录标注成本、质量、时延和人工修订成本 |

## 系统应用

### 14. VLM 在视觉算法中的应用

- **边界**：每个应用案例必须写传统基线、错误成本、人工复核、成本/延迟和失效条件；不扩成通用 VLM 或 Agent 知识库。
- **第一版关闭条件**：14A 完成，并至少两个真实案例通过独立验证；其余案例可继续保持 Pending。

| 子项 | 独立交付物 | 验收出口 |
|---|---|---|
| 14A | VLM 视觉应用评价与人工门协议 | 定义基线、样本、错误成本、人工复核、隐私、延迟和费用 |
| 14B | 数据筛选与预标注案例 | 与人工或传统模型基线比较，统计返工而非只展示样例 |
| 14C | 开放词汇识别案例 | 与待办 7 回链，区分模型能力与提示词偶然性 |
| 14D | 难例分析与评测辅助案例 | 结论必须由独立 evaluator 或人工复核，不由 VLM 自证 |
| 14E | 多模态推理案例 | 只有具体视觉任务出现后启动；不为展示能力而造题 |

### 15. VLA 应用简述

- **定位**：只说明视觉感知在 VLA / 具身系统中的位置、接口和约束。
- **目标**：厘清视觉编码、状态表征、动作生成、闭环反馈、数据和安全之间的基本关系，并筛选与本仓库视觉能力直接相关的应用。
- **边界**：保持“简述”；不建设机器人学、控制、世界模型或通用 VLA 百科。若形成独立问题域，转入专题仓库。
- **完成标志**：读者能区分 VLM、VLA 与普通视觉算法系统，并看清视觉模块能够复用什么、还缺什么证据。

扩散生成和 3D 视觉暂不进入正式待办，等待真实问题触发后再决定。
