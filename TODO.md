# TODO

这里维护已经确认、但尚未进入实施的仓库级任务。具体工作开始后，应转入对应主题目录或独立分支；本文件只保留任务状态、边界和验收出口，不扩展成方案文档，也不预建空目录。

## 分类说明

以下分组是四个正交视角，不是互斥目录树：

1. 架构模块：可复用的网络部件及接口；
2. 学习与对齐范式：模型如何获得或对齐表征；
3. 任务与完整模型方案：端到端解决什么视觉问题；
4. 系统应用：视觉能力如何进入更大的产品或智能系统。

同一个对象可以出现在多个视角，但只能有一个权威条目维护事实，其他位置通过关系链接引用。例如 HRNet 的高分辨率结构归入架构模块，HRNet-based heatmap 与 PFLD 的实验比较归入关键点任务；Meta DINO 归入自监督表征，检测器 DINO 归入 Detection → DETR。不能因为名称相同或都被称为“研究”，把对象放在同一类。

## Pending

### 1. 将 ProjectCollection 中仍有价值的内容融入 vision-workbench

- **背景**：[`visionzoo/projectcollection`](https://github.com/visionzoo/projectcollection) 的范围过于宽泛，长期维护困难。
- **目标**：把仍有价值且符合本仓库边界的内容，融入 [`visionzoo/vision-workbench`](https://github.com/visionzoo/vision-workbench)。
- **下一步**：先盘点、去重和归类，再按现有 Foundations、Engineering、Research、Productization 或独立项目边界分批迁移。
- **边界**：本任务不等于原目录整体复制；是否归档或删除 `projectcollection`，另行确认。

## 架构模块

### 2. Backbone 与 visual encoder

- **优先级**：高。
- **定位**：从特征提取与视觉编码角色组织 CNN、Transformer、混合结构和高分辨率表征方案。
- **目标**：建立结构机制、预训练来源、输出接口、尺度、计算/内存、量化部署与下游任务之间的映射。代表谱系至少覆盖 ResNet、MobileNet、EfficientNet、ConvNeXt、ViT / Swin、HRNet 与 TuringViT。
- **边界**：这是模块角色索引，不是所有网络都必须存在独立 backbone，也不把完整模型强拆成三段。是否建立独立 family 页面由真实使用、材料规模和独立维护价值决定。
- **完成标志**：能够在相同任务和评测条件下解释为何选择某类 backbone / visual encoder，并回链至少一个包含精度、速度和部署约束的真实对照案例。

### 3. Neck、feature fusion 与 adapter

- **定位**：组织中间特征的转换、融合与接口适配；FPN、PAN、BiFPN 只是其中的代表方案。
- **目标**：梳理单/多尺度输入、top-down / bottom-up 路径、融合算子、通道和分辨率对齐，以及跨模态或参数高效 adapter 的收益与代价。
- **边界**：`neck` 是常见工程称呼，不假定所有架构都有独立 neck；只在对象确实承担中间转换或融合角色时建立关系。
- **完成标志**：能够针对小目标、关键点、检测、分割或跨模态接口说明是否需要中间适配，并至少完成一次控制其他主要变量的消融验证。

### 4. Head 与 task decoder

- **定位**：组织把共享表征转换为任务输出的结构，包括分类、检测、关键点与分割输出；query-based decoder 等不强行改称传统 head。
- **目标**：比较分类 head、anchor-based / anchor-free 检测 head、heatmap / 坐标回归 / SimCC 关键点 head、mask head 与 query/task decoder，并关联损失、匹配、标签分配、解码和部署约束。
- **边界**：loss、matcher、后处理只建立关联，不因处于输出链路就都归为 head；跨越多个模块的端到端设计在完整任务方案中维护。
- **完成标志**：能够从任务误差反推输出表征与 task decoder/head 的选择，并至少完成一次固定上游表征后的可复现对照。

## 学习与对齐范式

### 5. 自监督视觉表征 DINO 家族

- **定位**：Meta 提出的自监督视觉表征谱系，与同名检测器 DINO 分开建设。
- **目标**：梳理 DINO、DINOv2、DINOv3 等正式版本的训练目标、表征性质、数据与训练差异、下游迁移方式和工程代价。
- **边界**：不混入检测器 DINO；与 ViT 等 visual encoder 共用的结构机制通过链接引用，不重复建设。
- **完成标志**：能够判断其何时适合作为视觉编码器或预训练来源，并用至少一个下游任务验证相对监督预训练或其他表征方案的收益与代价。

### 6. CLIP 家族与视觉—语言对齐

- **定位**：以 CLIP 谱系为入口建设视觉—语言对齐知识，同时区分训练范式、模型家族与编码器角色。
- **目标**：梳理对比学习目标、主要正式分支、零样本和开放词汇能力、微调方式及部署代价。
- **边界**：不把通用 VLM 全部并入 CLIP；视觉编码器结构回链待办 2，本条维护对齐目标、模型谱系及直接影响视觉算法选型的证据。
- **完成标志**：能够判断 CLIP 何时适合作为编码器、开放词汇接口或预训练来源，何时不如闭集专用模型。

## 任务与完整模型方案

### 7. Detection：YOLO、DETR 与检测器 DINO

- **优先级**：高。
- **定位**：从目标检测任务维护完整模型方案、架构谱系和横向选型，而不是把检测器拆散后只剩模块名单。
- **目标**：在已有 YOLO 内容之外建设 DETR 的集合预测、匹配与损失、query、收敛改进和主要分支；将检测器 DINO 作为 DETR 谱系中的独立家族建设，明确其与 Deformable DETR、DN-DETR 的继承关系。
- **边界**：DETR 总体范式与检测器 DINO 家族分别建设但共用 Detection 上下文；不因与 Meta DINO 同名而相邻归档，不复制 backbone、neck 或通用 Transformer 机制。
- **完成标志**：能够基于数据规模、目标尺度、实时性、训练和部署约束，在 YOLO、代表性 DETR 分支及检测器 DINO 之间做有证据的选择，并回链至少一个可复现验证。

### 8. 关键点与姿态估计

- **优先级**：高。
- **定位**：从关键点任务、输出表征和完整方案组织知识，再关联所用的 backbone、fusion、head 或 decoder。
- **目标**：比较 heatmap、坐标回归、SimCC 等表征，梳理可见性、遮挡、局部目标、多尺度、时序稳定性和轻量部署问题；HRNet-based heatmap、PFLD、RTMPose 等作为完整或可组合方案进入比较。
- **边界**：HRNet 的高分辨率结构机制回链待办 2；当前眼部关键点实验中 HRNet 方案优于 PFLD 只记为待补条件的系统级观察，不能归因于 backbone 或泛化为普遍结论。
- **完成标志**：用眼部关键点等真实任务比较至少两类方案，补齐数据、输入、训练、输出表征、模型规模、精度、不可见点处理、速度和板端部署条件。

### 9. 视频任务方案：跟踪、视频表征与事件时序

- **优先级**：中。
- **定位**：建立多目标跟踪、视频表征学习和事件时序判定之间的关系图；三者不是一个模型家族。
- **目标**：分别梳理三类问题的输入输出、评价指标、状态管理、工程接口及组合方式，并判断是否需要拆成独立待办。
- **边界**：不因为都处理视频就共享同一套模型分类；单帧无法解决的问题才引入时序状态和额外复杂度。
- **完成标志**：至少形成一个从单帧基线到时序方案的对照案例，说明收益、延迟、状态管理和失效模式，并据真实材料决定后续拆分。

### 10. 分割与可提示分割

- **优先级**：中。
- **定位**：从语义分割、实例分割和可提示分割的任务差异出发，组织完整方案及其模块关系。
- **目标**：梳理输出粒度、标注成本、评价指标、提示接口、自动掩码、蒸馏/轻量化，以及与检测和 VLM 的组合方式。
- **边界**：SAM 作为可提示分割家族进入本任务，不因基础模型热度扩成通用视觉基础模型百科。
- **完成标志**：能够判断检测框、传统分割与可提示分割各自的适用条件，并以至少一个真实视觉流程验证新增价值。

## 系统应用

### 11. VLM 在视觉算法中的应用

- **定位**：VLM 作为视觉算法系统中的能力组件，而不是独立的通用 Agent/VLM 知识库。
- **目标**：评估其在数据筛选与标注、开放词汇识别、难例分析、评测辅助和多模态推理中的实际价值。
- **边界**：每个应用必须写清传统方案基线、错误成本、人工复核点、成本/延迟和失效条件；模型结构与对齐范式回链相应条目。
- **完成标志**：至少形成一个可独立验证的视觉任务案例，而不是停留在应用清单或演示效果。

### 12. VLA 应用简述

- **定位**：只说明视觉感知在 VLA / 具身系统中的位置、接口和约束。
- **目标**：厘清视觉编码、状态表征、动作生成、闭环反馈、数据和安全之间的基本关系，并筛选与本仓库视觉能力直接相关的应用。
- **边界**：保持“简述”；不建设机器人学、控制、世界模型或通用 VLA 百科。若形成独立问题域，转入专题仓库。
- **完成标志**：读者能区分 VLM、VLA 与普通视觉算法系统，并看清视觉模块能够复用什么、还缺什么证据。

扩散生成和 3D 视觉暂不进入正式待办，等待真实问题触发后再决定。
