# TODO

这里维护已经确认、但尚未进入实施的仓库级任务。具体工作开始后，应转入对应主题目录或独立分支；本文件只保留任务状态、边界和验收出口，不扩展成方案文档，也不预建空目录。

## Pending

### 1. 将 ProjectCollection 中仍有价值的内容融入 vision-workbench

- **背景**：[`visionzoo/projectcollection`](https://github.com/visionzoo/projectcollection) 的范围过于宽泛，长期维护困难。
- **目标**：把仍有价值且符合本仓库边界的内容，融入 [`visionzoo/vision-workbench`](https://github.com/visionzoo/vision-workbench)。
- **下一步**：先盘点、去重和归类，再按现有 Foundations、Engineering、Research、Productization 或独立项目边界分批迁移。
- **边界**：本任务不等于原目录整体复制；是否归档或删除 `projectcollection`，另行确认。

## 架构模块

### 2. Backbone 模块

- **优先级**：高。
- **定位**：按“特征提取 / 视觉编码”这一稳定模块角色，统一组织 CNN、Transformer、混合结构和高分辨率表征方案。
- **目标**：建立结构机制、预训练方式、输出尺度、计算/内存、量化部署与下游任务之间的映射。代表谱系至少覆盖 ResNet、MobileNet、EfficientNet、ConvNeXt、ViT / Swin、HRNet 与 TuringViT；是否建立独立 family 页面由真实使用和材料规模决定。
- **边界**：不把所有模型压成一个排名表；同名架构作为完整 task model 使用时，需要同时记录 head 与任务设置。DINO、CLIP 等训练/对齐体系保留独立对象，只回链其采用的 backbone。
- **当前触发**：眼部关键点实验中观察到 HRNet 比 PFLD 效果好。该信息目前是本人实验观察，不是“HRNet 普遍优于 PFLD”的结论；需要补齐数据 split、输入、输出表征、训练设置、模型规模、精度和速度后再沉淀。
- **完成标志**：能够在相同任务与评测条件下解释为何选择某类 backbone，并回链至少一个包含精度、速度与部署约束的真实对照案例。

### 3. Neck 模块

- **定位**：位于 backbone 与 head 之间，承担跨层、跨尺度或跨模态特征转换与融合。
- **目标**：围绕单/多尺度输入、top-down / bottom-up 路径、融合算子、通道与分辨率对齐，梳理 FPN、PAN、BiFPN 及代表性 Transformer neck 的收益与代价。
- **边界**：不脱离上下游任务孤立比较 FLOPs；若某个 neck 只在特定完整架构中成立，保留对该架构的关系链接，不复制整套模型内容。
- **完成标志**：能够针对小目标、关键点、检测或分割任务说明是否需要 neck、选择何种融合方式，并至少完成一次控制 backbone 与 head 的消融验证。

### 4. Head 模块

- **定位**：把共享特征转换为任务输出的模块，包括分类、检测、关键点与分割等不同输出表征。
- **目标**：按输出语义组织分类 head、anchor-based / anchor-free / query-based 检测 head、heatmap / 坐标回归 / SimCC 关键点 head 和 mask head，并关联损失、标签分配、解码与部署约束。
- **边界**：loss、matcher、后处理等只有在结构上确属 head 时才并入；DETR decoder/query、端到端 NMS-free 等跨越多个模块的设计仍在对应任务范式维护。
- **完成标志**：能够从任务误差反推 head 与输出表征选择，并至少在固定 backbone / neck 的条件下完成一次可复现对照。

## 表征学习与多模态

### 5A. 自监督视觉表征 DINO 家族

- **定位**：Meta 提出的自监督视觉表征谱系，与同名检测器 DINO 分开建设。
- **目标**：梳理 DINO、DINOv2、DINOv3 等正式版本的训练目标、表征性质、数据与训练差异、下游迁移方式和工程代价。
- **边界**：不混入检测器 DINO；与 ViT 等 backbone 共用的基础机制通过链接引用，不重复建设。
- **完成标志**：能够判断其何时适合作为视觉编码器或预训练来源，并用至少一个下游任务验证相对监督预训练或其他表征模型的收益与代价。

### 5B. 检测器 DINO 家族

- **定位**：DETR 检测谱系中的 DINO 分支，与 Meta 自监督 DINO 分开建设。
- **目标**：梳理从 DETR、Deformable DETR、DN-DETR 到 DINO 检测器的机制继承、版本关系、训练收敛、精度/速度和部署条件。
- **边界**：本条维护 DINO 检测器家族细节；待办 9 只维护 DETR 总体范式与跨家族比较，二者通过链接关联，不复制内容。
- **完成标志**：能够解释检测器 DINO 相比 YOLO 与其他 DETR 分支的适用条件，并回链至少一个可复现训练、评测或部署验证。

### 6. CLIP 家族

- **定位**：视觉—语言对齐模型与可复用视觉/文本编码器。
- **目标**：梳理对比学习目标、主要正式分支、零样本与开放词汇能力、微调方式及部署代价。
- **边界**：不把通用 VLM 全部并入 CLIP；backbone 结构回链待办 2，只保留 CLIP 对齐体系及对视觉算法选型有直接作用的比较。
- **完成标志**：能够判断 CLIP 何时适合作为编码器、开放词汇接口或预训练来源，何时不如闭集专用模型。

### 7. VLM 在视觉算法中的应用

- **定位**：VLM 作为视觉算法系统中的能力组件，而不是独立的通用 Agent/VLM 知识库。
- **目标**：评估其在数据筛选与标注、开放词汇识别、难例分析、评测辅助和多模态推理中的实际价值。
- **边界**：每个应用必须写清传统方案基线、错误成本、人工复核点、成本/延迟和失效条件；若扩展为通用 VLM 专题，应拆到独立仓库。
- **完成标志**：至少形成一个可独立验证的视觉任务案例，而不是停留在应用清单或演示效果。

### 8. VLA 应用简述

- **定位**：只说明视觉感知在 VLA / 具身系统中的位置、接口和约束。
- **目标**：厘清视觉编码、状态表征、动作生成、闭环反馈、数据和安全之间的基本关系，并筛选与本仓库视觉能力直接相关的应用。
- **边界**：保持“简述”；不建设机器人学、控制、世界模型或通用 VLA 百科。若形成独立问题域，转入专题仓库。
- **完成标志**：读者能区分 VLM、VLA 与普通视觉算法系统，并看清视觉模块能够复用什么、还缺什么证据。

## 任务与系统范式

### 9. DETR 家族

- **优先级**：高。
- **定位**：YOLO 之外的集合预测 / Transformer 检测范式。
- **目标**：梳理 DETR 的核心问题表征、匹配与损失、查询机制、收敛改进、主要分支，以及 backbone、neck、decoder/head 的组合关系。
- **边界**：维护 DETR 总体机制、分支地图和横向选型；检测器 DINO 的家族细节归待办 5B，不在此重复。
- **完成标志**：能够基于数据规模、目标尺度、实时性、训练和部署约束，在 YOLO 与代表性 DETR 分支之间做有证据的选择。

### 10. 关键点与姿态估计

- **优先级**：高。
- **定位**：从关键点任务、输出表征及 backbone / neck / head 组合出发组织方案。
- **目标**：比较 heatmap、坐标回归、SimCC 等表征，梳理可见性、遮挡、局部目标、多尺度、时序稳定性和轻量部署问题；HRNet、PFLD、RTMPose 等只作为带明确角色的代表实现。
- **边界**：人脸、人体和局部关键点先共享任务机制；HRNet 的高分辨率 backbone 机制归待办 2，本条保留完整任务方案和实验比较。不同输出表征或模型规模未经控制时，不把单次效果差异解释成架构普遍优劣。
- **完成标志**：用眼部关键点等真实任务比较至少两类方案，补齐数据、输入、训练、精度、不可见点处理、速度与板端部署条件。

### 11. 视频时序与跟踪

- **优先级**：中。
- **定位**：处理单帧视觉无法解决的关联、状态变化和事件判定问题。
- **目标**：分别梳理多目标跟踪、视频表征学习和事件时序判定的输入输出、评价指标、工程状态与组合方式。
- **边界**：三者不因都处理视频就混成一个模型家族；先建立关系和选择边界，真实案例出现后再决定是否拆分。
- **完成标志**：至少形成一个从单帧基线到时序方案的对照案例，说明收益、延迟、状态管理和失效模式。

### 12. 分割与可提示分割

- **优先级**：中。
- **定位**：从语义分割、实例分割和可提示分割任务差异出发，再组织代表模型及其 backbone / neck / head 组合。
- **目标**：梳理输出粒度、标注成本、评价指标、提示接口、自动掩码、蒸馏/轻量化及与检测/VLM 的组合方式。
- **边界**：SAM 作为可提示分割家族进入本任务，不因基础模型热度单独扩成通用视觉基础模型百科。
- **完成标志**：能够判断检测框、传统分割与可提示分割各自适用条件，并以至少一个真实视觉流程验证其新增价值。

扩散生成和 3D 视觉暂不进入正式待办，等待真实问题触发后再决定。