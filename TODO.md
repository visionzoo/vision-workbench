# TODO

这里维护已经确认、但尚未进入实施的仓库级任务。具体工作开始后，应转入对应主题目录或独立分支；本文件只保留任务状态、边界和验收出口，不扩展成方案文档，也不预建空目录。

## Pending

### 1. 将 ProjectCollection 中仍有价值的内容融入 vision-workbench

- **背景**：[`visionzoo/projectcollection`](https://github.com/visionzoo/projectcollection) 的范围过于宽泛，长期维护困难。
- **目标**：把仍有价值且符合本仓库边界的内容，融入 [`visionzoo/vision-workbench`](https://github.com/visionzoo/vision-workbench)。
- **下一步**：先盘点、去重和归类，再按现有 Foundations、Engineering、Research、Productization 或独立项目边界分批迁移。
- **边界**：本任务不等于原目录整体复制；是否归档或删除 `projectcollection`，另行确认。

### 2. MobileNet 家族

- **定位**：轻量 CNN task model / backbone。
- **目标**：梳理官方谱系、关键轻量化机制、训练与迁移方式，以及移动端和 NPU 部署时真正影响选型的条件。
- **边界**：不扩展成“所有轻量网络”百科；ResNet、EfficientNet、ConvNeXt 等只作为必要比较基线。
- **完成标志**：能够回答不同任务与硬件约束下为什么选择或不选择 MobileNet，并回链至少一个真实工程验证。

### 3. DINO 家族

- **目标**：沉淀 DINO 所代表的方法、版本关系、视觉表征能力、下游使用方式和工程边界。
- **未决边界**：`DINO` 同时可能指 Meta 的自监督视觉表征家族，也可能指 DETR 系检测器 DINO；两者不是同一模型族。开始建设前必须由本人确认具体所指，不得把两条谱系混写。
- **完成标志**：对象归属明确，官方事实、第三方复现与本人验证分开，并能说明它与 ViT、CLIP 及下游任务的关系。

### 4. CLIP 家族

- **定位**：视觉—语言对齐模型与可复用视觉/文本编码器。
- **目标**：梳理对比学习目标、主要正式分支、零样本与开放词汇能力、微调方式及部署代价。
- **边界**：不把通用 VLM 全部并入 CLIP；只保留 CLIP 谱系及对视觉算法选型有直接作用的比较。
- **完成标志**：能够判断 CLIP 何时适合作为编码器、开放词汇接口或预训练来源，何时不如闭集专用模型。

### 5. ViT 家族

- **定位**：通用视觉 Transformer 基线与主要架构分支。
- **目标**：建立 patch/token、位置编码、注意力、层级与分辨率处理等机制到模型选择和部署约束的映射。
- **边界**：不穷举所有带 Transformer 的视觉模型；DINO、CLIP 和 TuringViT 保留各自对象边界，通过关系链接进入 ViT 图谱，不复制内容。
- **完成标志**：能够把 CNN 与 ViT、全局与层级结构、固定与动态分辨率的差异落到实际任务和硬件条件。

### 6. VLM 在视觉算法中的应用

- **定位**：VLM 作为视觉算法系统中的能力组件，而不是独立的通用 Agent/VLM 知识库。
- **目标**：评估其在数据筛选与标注、开放词汇识别、难例分析、评测辅助和多模态推理中的实际价值。
- **边界**：每个应用必须写清传统方案基线、错误成本、人工复核点、成本/延迟和失效条件；若扩展为通用 VLM 专题，应拆到独立仓库。
- **完成标志**：至少形成一个可独立验证的视觉任务案例，而不是停留在应用清单或演示效果。

### 7. VLA 应用简述

- **定位**：只说明视觉感知在 VLA / 具身系统中的位置、接口和约束。
- **目标**：厘清视觉编码、状态表征、动作生成、闭环反馈、数据和安全之间的基本关系，并筛选与本仓库视觉能力直接相关的应用。
- **边界**：保持“简述”；不建设机器人学、控制、世界模型或通用 VLA 百科。若形成独立问题域，转入专题仓库。
- **完成标志**：读者能区分 VLM、VLA 与普通视觉算法系统，并看清视觉模块能够复用什么、还缺什么证据。

## Candidates — 待本人确认

以下条目来自当前能力覆盖检查，只是候选，不代表已经承诺建设，也不创建目录。

- **DETR 家族（高优先级）**：补足 YOLO 之外的集合预测 / Transformer 检测范式；如果待办 3 指的是检测器 DINO，应归入此项，而不是自监督 DINO。
- **关键点与姿态估计（高优先级）**：围绕 heatmap、坐标回归、可见性与轻量部署组织 HRNet、PFLD、RTMPose 等代表方案；与当前 DMS 眼部定位问题直接相关。
- **视频时序与跟踪（中优先级）**：区分多目标跟踪、视频表征学习和事件时序判定，不把三者混成一个“视频模型家族”。
- **分割与可提示分割（中优先级）**：从语义/实例分割任务出发，再判断是否建立 SAM 家族条目；避免因基础模型热度直接扩目录。

暂不建议单列 ResNet、EfficientNet、ConvNeXt、扩散生成、3D 视觉等待办；前几者先作为 backbone 比较基线，后几者等真实问题触发后再决定。
